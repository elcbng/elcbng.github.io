# BNG 技术博客

Forked from mzlogin

## How to use

```shell
#shell with git & ruby installed

git clone [repo clone url]
cd elcbng.github.io
gem install bundler
bundle install
bundle exec jekyll serve
```